{#
/**
 * @file
 * Overrides: page-html.twig
 *
 * The doctype, html, head and body tags are not in this template. Instead they
 * can be found in the html.html.twig template in this directory.
 *
 * Available variables:
 *
 * General utility variables:
 * - base_path: The base URL path of the Drupal installation. Will usually be
 *   "/" unless you have installed Drupal in a sub-directory.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and signed in.
 * - is_admin: A flag indicating if the user has permission to access
 *   administration pages.
 *
 * Site identity:
 * - front_page: The URL of the front page. Use this instead of base_path when
 *   linking to the front page. This includes the language domain or prefix.
 *
 * Page content (in order of occurrence in the default page.html.twig):
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - messages: Status and error messages. Should be displayed prominently.
 * - tabs: Tabs linking to any sub-pages beneath the current page (e.g., the
 *   view and edit tabs when displaying a node).
 * - node: Fully loaded node, if there is an automatically-loaded node
 *   associated with the page and the node ID is the second argument in the
 *   page's path (e.g. node/12345 and node/12345/revisions, but not
 *   comment/reply/12345).
 *
 * Regions:
 * - page.header: Items for the header region. Included from header.html.twig
 * - page.top_toolbar: Items for the top navigation region, includes social
 *     media icons, quick links, translate, etc. Included from header.html.twig
 * - page.main_navigation: Main navigation region. Also includes search.
 *     Included from header.html.twig
 * - page.home_carousel: Items for the home carousel region. Included from
 *     carousel.html.twig
 * - page.content: The main content of the current page.
 * - page.footer: Items for the footer region.
 */
#}

{% set container = theme.settings.fluid_container ? 'container-fluid' : 'container' %}

{# Announcements #}
{% include district_theme_directory ~ '/templates/regions/announcements.html.twig' %}
{# Main header #}
{% include district_theme_directory ~ '/templates/regions/header.html.twig' %}

{# Main Content #}
<div role="main" class="content-wrapper--audience-page main-container js-quickedit-main-content">
  <a id="main-content"></a>
  <div class="{{ container }}">
    <div class="row content-section clearfix wrapper--slideshow-and-calendar">
      {# Slideshow #}
      {% include district_theme_directory ~ '/templates/regions/home-page-slideshow.html.twig' %}
      {# Calendar #}
      {% include district_theme_directory ~ '/templates/regions/calendar.html.twig' %}
    </div>
    <div class="row content-section clearfix">
      {# Featured #}
      {% include district_theme_directory ~ '/templates/regions/featured-pages.html.twig' %}
      {# Ad Space #}
      {% include district_theme_directory ~ '/templates/regions/adspace.html.twig' %}
    </div>
    <div class="row content-section clearfix">
      {# Top Stories #}
      {% include district_theme_directory ~ '/templates/regions/top-stories.html.twig' %}
    </div>
  </div>
  <div class="wrapper--highlights text-center clearfix">
    {# Highlights #}
    {% include district_theme_directory ~ '/templates/regions/highlights.html.twig' %}
  </div>
  <div class="{{ container }}">
    <div class="row content-section clearfix">
      <div class="col-xs-12">
        <div class="row wrapper--featured-video-and-social-media">
          {# Featured Video #}
          {% include district_theme_directory ~ '/templates/regions/featured-video.html.twig' %}
          {# Social Media #}
          {% include district_theme_directory ~ '/templates/regions/social-media.html.twig' %}
        </div>
      </div>
    </div>
  </div>
</div>

{# District Programs #}
{% include district_theme_directory ~ '/templates/regions/district-programs.html.twig' %}

{# Footer #}
{% include district_theme_directory ~ '/templates/regions/footer.html.twig' %}
